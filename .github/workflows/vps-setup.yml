name: VPS setup scripts

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test-setup-vps:
    name: Test VPS bootstrap script
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Start shared PostgreSQL on infra network
        working-directory: docs/example-postgresql-docker-compose
        env:
          POSTGRES_PASSWORD: postgres
        run: |
          cp .env.example .env
          sed -i 's/change-me-postgres-password/postgres/' .env
          docker compose up -d
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U postgres -d weather -t 1 && break
            sleep 1
          done

      - name: Run setup script (CI mode)
        env:
          DEPLOY_DIR: /tmp/flask-weather-deploy
          SETUP_SKIP_APT: "1"
          SETUP_SKIP_DOCKER_INSTALL: "1"
          SETUP_SOURCE_DIR: ${{ github.workspace }}
          DATABASE_URL: postgresql://postgres:postgres@postgresql:5432/weather
          SECRET_KEY: ci-test-secret
          HEALTH_TIMEOUT_SEC: "180"
        run: |
          chmod +x scripts/setup-vps.sh
          sudo -E bash scripts/setup-vps.sh

      - name: Verify health endpoint
        run: |
          curl -sf http://127.0.0.1:5000/health | grep -q '"status":"ok"'

      - name: Verify deploy env file
        run: |
          test -f /tmp/flask-weather-deploy/.env
          grep -q '^SECRET_KEY=ci-test-secret' /tmp/flask-weather-deploy/.env
          grep -q '^DATABASE_URL=' /tmp/flask-weather-deploy/.env

      - name: Run setup script again (idempotency)
        env:
          DEPLOY_DIR: /tmp/flask-weather-deploy
          SETUP_SKIP_APT: "1"
          SETUP_SKIP_DOCKER_INSTALL: "1"
          SETUP_SOURCE_DIR: ${{ github.workspace }}
          DATABASE_URL: postgresql://postgres:postgres@postgresql:5432/weather
          HEALTH_TIMEOUT_SEC: "180"
        run: |
          sudo -E bash scripts/setup-vps.sh 2>&1 | tee /tmp/setup-run2.log
          grep -q 'skipping redeploy' /tmp/setup-run2.log

      - name: Verify SECRET_KEY persisted across runs
        run: grep -q '^SECRET_KEY=ci-test-secret' /tmp/flask-weather-deploy/.env

      - name: Show container status on failure
        if: failure()
        working-directory: /tmp/flask-weather-deploy
        run: docker compose ps && docker compose logs --tail=100

  test-setup-postgresql:
    name: Test PostgreSQL bootstrap script
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run setup script (CI mode)
        env:
          DEPLOY_DIR: /tmp/postgresql-deploy
          SETUP_SKIP_APT: "1"
          SETUP_SKIP_DOCKER_INSTALL: "1"
          SETUP_SOURCE_DIR: ${{ github.workspace }}/docs/example-postgresql-docker-compose
          POSTGRES_PASSWORD: ci-test-postgres-password
          PG_READY_TIMEOUT_SEC: "120"
        run: |
          chmod +x docs/example-postgresql-docker-compose/scripts/setup-vps.sh
          sudo -E bash docs/example-postgresql-docker-compose/scripts/setup-vps.sh

      - name: Verify PostgreSQL is ready
        working-directory: /tmp/postgresql-deploy
        run: |
          docker compose ps --status running | grep -q postgres
          docker compose exec -T postgres pg_isready -U postgres -d postgres

      - name: Verify weather database was created
        working-directory: /tmp/postgresql-deploy
        env:
          PGPASSWORD: ci-test-postgres-password
        run: |
          docker compose exec -T postgres psql -U postgres -d postgres -tAc \
            "SELECT 1 FROM pg_database WHERE datname = 'weather'" | grep -q 1

      - name: Verify deploy env file
        run: |
          test -f /tmp/postgresql-deploy/.env
          grep -q '^POSTGRES_PASSWORD=ci-test-postgres-password' /tmp/postgresql-deploy/.env
          grep -q '^POSTGRES_USER=postgres' /tmp/postgresql-deploy/.env
          grep -q '^POSTGRES_PORT=5432' /tmp/postgresql-deploy/.env

      - name: Run setup script again (idempotency)
        env:
          DEPLOY_DIR: /tmp/postgresql-deploy
          SETUP_SKIP_APT: "1"
          SETUP_SKIP_DOCKER_INSTALL: "1"
          SETUP_SOURCE_DIR: ${{ github.workspace }}/docs/example-postgresql-docker-compose
          POSTGRES_PASSWORD: ci-test-postgres-password
          PG_READY_TIMEOUT_SEC: "120"
        run: |
          sudo -E bash docs/example-postgresql-docker-compose/scripts/setup-vps.sh 2>&1 | tee /tmp/pg-setup-run2.log
          grep -q 'skipping redeploy' /tmp/pg-setup-run2.log

      - name: Verify POSTGRES_PASSWORD persisted across runs
        run: grep -q '^POSTGRES_PASSWORD=ci-test-postgres-password' /tmp/postgresql-deploy/.env

      - name: Show container status on failure
        if: failure()
        working-directory: /tmp/postgresql-deploy
        run: docker compose ps && docker compose logs --tail=100 postgres
