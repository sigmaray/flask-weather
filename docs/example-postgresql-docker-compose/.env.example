POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-postgres-password

# Host port (bound to 127.0.0.1 only — for host-native apps).
POSTGRES_PORT=5432

# flask-weather DATABASE_URL example (Docker app on network infra):
# postgresql://postgres:<POSTGRES_PASSWORD>@postgresql:5432/weather
