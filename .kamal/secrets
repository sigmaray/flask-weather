# Secrets are read from the environment (GitHub Actions secrets or local shell).
# Do not put raw credentials in this file.

KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
SECRET_KEY=$SECRET_KEY
POSTGRES_PASSWORD=$POSTGRES_PASSWORD
DATABASE_URL=postgresql://weather:${POSTGRES_PASSWORD}@flask-weather-db:5432/weather
